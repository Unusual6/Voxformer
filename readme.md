### 使用说明

训练模型命令为：`tools/dist_train.sh CONFIG_path 2`；<br>
测试模型命令为：`tools/dist_test.sh CONFIG_path CHECKPOINT_path 2`。<br>
可视化系统使用方法如下：进入目录 `cd tools/vis_system`，<br>
然后运行 `python main.py`，<br>
最后在浏览器中打开 [http://127.0.0.1:5000](http://127.0.0.1:5000) 查看结果。
